export default function Loading() {
  return (
    <div className="fade-in" style={{ padding: '64px 24px', textAlign: 'center' }}>
      <span className="eyebrow muted">Loading…</span>
    </div>
  );
}
