# dependencies
node_modules

# next.js
.next
out
dist

# local data store (fallback when Firebase isn't configured)
.data

# env
.env
.env*.local

# misc
*.local
.DS_Store
.claude

# Firebase service account keys — NEVER commit these private keys
*-firebase-adminsdk-*.json
firebase-service-account*.json

# extracted design handoff bundle (reference only)
_design
.vercel
